---
# ==============================================================================
# vuln_lateral/defaults/main.yml
# ==============================================================================

# empire.local domain DN (used for New-ADUser/ACL -Path targets)
ad_domain_dn: "DC=empire,DC=local"

# Attacker-controlled IP used in UNC coercion bait (.url IconFile, etc.)
attacker_ip: "10.10.0.1"
