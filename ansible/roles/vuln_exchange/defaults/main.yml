---
# ==============================================================================
# vuln_exchange/defaults/main.yml — SRV-001..080
# ==============================================================================

ad_domain:    "empire.local"
ad_domain_dn: "DC=empire,DC=local"

# SQL Server service account
sql_sa_password: "DeathStar2025!"
sql_svc_password: "SithLord123!"

# SCCM stub config
sccm_site_code:    "DVA"
sccm_site_server:  "scarif.empire.local"
sccm_naa_account:  "EMPIRE\\svc_sccm"

# WSUS server (stub — scarif acts as WSUS)
wsus_server: "scarif.empire.local"
wsus_port:   8530

# Exchange stub — no real Exchange installed; note-based surfaces
exchange_fqdn: "mail.empire.local"
exchange_ip:   "10.10.0.13"

# Fake mailbox accounts
exchange_mailbox_accounts:
  - name: "svc_exchange"
    password: "Exchange2024!"
  - name: "mail_admin"
    password: "MailAdmin2024!"
