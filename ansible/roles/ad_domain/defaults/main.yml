---
# ==============================================================================
# ad_domain role defaults
# Override per-host in inventory group_vars or host_vars
# ==============================================================================

ad_domain: "empire.local"
ad_netbios: "EMPIRE"
ad_password: "SithLord123!"
ad_safe_mode_password: "SithLord123!"

# DC IP used for LDAP readiness checks by downstream roles
ad_dc_ip: "10.10.0.10"

# Functional level — WinThreshold = Server 2016+
ad_forest_mode: "WinThreshold"
ad_domain_mode: "WinThreshold"

# NTDS/SYSVOL paths
ad_db_path: 'C:\Windows\NTDS'
ad_log_path: 'C:\Windows\NTDS'
ad_sysvol_path: 'C:\Windows\SYSVOL'
