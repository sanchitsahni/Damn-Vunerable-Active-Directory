---
# ==============================================================================
# ad_domain/tasks/main.yml
# Promotes a Windows Server to DC for the domain defined in defaults/main.yml.
# Called three times from site.yml: empire.local, rebel.local, trade.corp —
# each with overriding vars.
# ==============================================================================

# --------------------------------------------------------------------------
# Network — set static IP before anything AD-related
# --------------------------------------------------------------------------

- name: Schedule static IP + DNS change (detached; survives WinRM drop)
  # Async fire-and-forget over WinRM rides the current session to launch the
  # job and read its handle; the netsh IP flip severs that in-flight Send and
  # pywinrm returns "Bad HTTP response ... Code 400". A SYSTEM scheduled task
  # detaches the change so the WinRM call returns cleanly before the drop.
  ansible.windows.win_shell: |
    $script = @'
    Start-Sleep -Seconds 5
    $adapter = Get-NetAdapter | Where-Object { $_.Status -eq "Up" } | Select-Object -First 1
    netsh interface ip set address "$($adapter.Name)" static {{ ad_static_ip }} {{ ad_subnet_mask }} {{ ad_gateway }} 1
    netsh interface ip set dns    "$($adapter.Name)" static {{ ad_static_ip }}
    '@
    $path = "C:\Windows\Temp\set-static-ip.ps1"
    Set-Content -Path $path -Value $script -Encoding ASCII
    $action  = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoProfile -ExecutionPolicy Bypass -File $path"
    $trigger = New-ScheduledTaskTrigger -Once -At (Get-Date).AddSeconds(3)
    Register-ScheduledTask -TaskName "EmpireSetStaticIP" -Action $action -Trigger $trigger `
      -RunLevel Highest -User "SYSTEM" -Force | Out-Null
  changed_when: false   # netsh is not idempotent, but we always want it applied

- name: Wait for network to stabilize after IP change
  # Low-RAM Server Core DCs (1280 MB finance/root) take longer to restore WinRM
  # after the DHCP->static switch; 60 s was too short and failed reconnect.
  ansible.builtin.wait_for_connection:
    delay: 30
    timeout: 240

- name: Reset WinRM connection to clear stale sockets after IP change
  ansible.builtin.meta: reset_connection

# --------------------------------------------------------------------------
# Hostname
# --------------------------------------------------------------------------

- name: Set hostname
  ansible.windows.win_hostname:
    name: "{{ ad_hostname }}"
  register: hostname_result

# Apply the rename with a reboot BEFORE the heavy AD DS work. Installing
# AD-Domain-Services while a hostname change is still pending is what severs
# WinRM mid-command on RAM-constrained, parallel-promoting DCs.
- name: Reboot to apply hostname change before AD DS install
  ansible.windows.win_reboot:
    reboot_timeout: 600
    post_reboot_delay: 30
  when: hostname_result.reboot_required | default(false)

# --------------------------------------------------------------------------
# Feature installation
# --------------------------------------------------------------------------

# Features are usually baked by the autounattend, so this is normally a quick
# state check. On a busy DC (3 promoting in parallel, low RAM) WinRM can still
# be severed mid-command — and that surfaces as an UNREACHABLE read-timeout,
# which a block/rescue does NOT catch (rescue only fires on task *failure*).
# So tolerate it explicitly, reconnect, and retry (the retry is a no-op confirm
# once the box settles).
- name: Install AD DS features
  ansible.windows.win_feature:
    name: &ad_ds_features
      - AD-Domain-Services
      - DNS
      - GPMC
      - RSAT-ADDS
      - RSAT-DNS-Server
      - RSAT-AD-PowerShell
    state: present
    include_management_tools: yes
  register: ad_ds_install
  ignore_unreachable: true
  ignore_errors: true

- name: Recover WinRM and confirm AD DS features after a mid-command drop
  when: ad_ds_install is unreachable or ad_ds_install is failed
  block:
    - name: Wait for WinRM to come back
      ansible.builtin.wait_for_connection:
        delay: 15
        timeout: 600
    - name: Reset stale WinRM connection
      ansible.builtin.meta: reset_connection
    - name: Install AD DS features (retry — confirms baked features)
      ansible.windows.win_feature:
        name: *ad_ds_features
        state: present
        include_management_tools: yes
      register: ad_ds_install

- name: Reboot if feature install requires it
  ansible.windows.win_reboot:
    reboot_timeout: 600
    post_reboot_delay: 30
  when: ad_ds_install.reboot_required | default(false)

# --------------------------------------------------------------------------
# Idempotency gate — skip promotion if DC role already held
# DomainRole values: 4 = BackupDC, 5 = PrimaryDC
# --------------------------------------------------------------------------

- name: Check current domain role
  ansible.windows.win_shell: |
    (Get-CimInstance -ClassName Win32_ComputerSystem).DomainRole
  register: domain_role
  changed_when: false

# --------------------------------------------------------------------------
# Forest promotion (async — server reboots mid-execution)
# --------------------------------------------------------------------------

- name: Promote to DC — create new forest {{ ad_domain }}
  ansible.windows.win_shell: |
    Import-Module ADDSDeployment
    $params = @{
      DomainName                    = "{{ ad_domain }}"
      DomainNetbiosName             = "{{ ad_netbios }}"
      ForestMode                    = "{{ ad_forest_mode }}"
      DomainMode                    = "{{ ad_domain_mode }}"
      DatabasePath                  = "{{ ad_db_path }}"
      LogPath                       = "{{ ad_log_path }}"
      SysvolPath                    = "{{ ad_sysvol_path }}"
      SafeModeAdministratorPassword = (ConvertTo-SecureString "{{ ad_safe_mode_password }}" -AsPlainText -Force)
      InstallDns                    = $true
      CreateDnsDelegation           = $false
      # NoRebootOnCompletion=$false lets the DC reboot itself; Ansible reconnects below
      NoRebootOnCompletion          = $false
      Force                         = $true
    }
    Install-ADDSForest @params
  register: forest_promotion
  async: 1800
  poll: 0
  when: domain_role.stdout | trim not in ['4', '5']

# Switch to domain-qualified credentials before the reboot completes so
# Ansible can authenticate against the new domain SAM database.
- name: Switch Ansible credentials to domain Administrator
  ansible.builtin.set_fact:
    ansible_user: "Administrator@{{ ad_domain }}"
  when: domain_role.stdout | trim not in ['4', '5']

- name: Wait for DC to reboot and WinRM to return (up to 15 min)
  ansible.builtin.wait_for_connection:
    delay: 60
    timeout: 900
  when: domain_role.stdout | trim not in ['4', '5']

# --------------------------------------------------------------------------
# OU structure
# --------------------------------------------------------------------------

- name: Wait for AD Web Services (ADWS) to answer before OU/user tasks
  # Import-Module alone succeeds the moment the module files exist, BEFORE ADWS
  # is listening — a useless gate that let downstream queries hit a dead ADWS
  # and fail with "Unable to find a default server ... ADWS running" (err 1355).
  # Probe the live service via Get-ADDomain against the local DC instead.
  ansible.windows.win_shell: |
    Import-Module ActiveDirectory -ErrorAction Stop
    (Get-ADDomain -Server localhost -ErrorAction Stop).DNSRoot
  register: ad_module_check
  changed_when: false
  retries: 30
  delay: 20
  until: ad_module_check.rc == 0

- name: Create base OU structure
  ansible.windows.win_shell: |
    Import-Module ActiveDirectory
    # Pin every AD cmdlet to the local DC ADWS endpoint; avoids DNS-locator races
    $PSDefaultParameterValues = @{ '*-AD*:Server' = 'localhost' }
    $base = "DC={{ ad_domain.split('.') | join(',DC=') }}"

    $ous = @(
      @{ Name = "Corp";            Path = $base },
      @{ Name = "Users";           Path = "OU=Corp,$base" },
      @{ Name = "Groups";          Path = "OU=Corp,$base" },
      @{ Name = "Computers";       Path = "OU=Corp,$base" },
      @{ Name = "ServiceAccounts"; Path = "OU=Corp,$base" },
      @{ Name = "Servers";         Path = "OU=Corp,$base" },
      @{ Name = "Workstations";    Path = "OU=Corp,$base" }
    )

    foreach ($ou in $ous) {
      $existing = Get-ADOrganizationalUnit -Filter "Name -eq '$($ou.Name)'" -SearchBase $ou.Path -SearchScope OneLevel -ErrorAction SilentlyContinue
      if (-not $existing) {
        New-ADOrganizationalUnit -Name $ou.Name -Path $ou.Path -ProtectedFromAccidentalDeletion $false
        Write-Output "Created OU: $($ou.Name)"
      } else {
        Write-Output "OU exists: $($ou.Name)"
      }
    }
  register: ou_result
  changed_when: "'Created OU' in ou_result.stdout"

# --------------------------------------------------------------------------
# Sub-task imports (users and groups kept in separate files for readability)
# --------------------------------------------------------------------------

- name: Import user creation tasks
  ansible.builtin.import_tasks: users.yml
  when: ad_domain == "empire.local"

- name: Import group creation tasks
  ansible.builtin.import_tasks: groups.yml
  when: ad_domain == "empire.local"

# Finance-specific users/groups (minimal set for cross-forest attack paths)
- name: Create rebel.local OUs and users
  ansible.windows.win_shell: |
    Import-Module ActiveDirectory
    $PSDefaultParameterValues = @{ '*-AD*:Server' = 'localhost' }
    $base = "DC=rebel,DC=local"
    foreach ($ouName in @("Finance","ServiceAccounts","Computers")) {
      if (-not (Get-ADOrganizationalUnit -Filter "Name -eq '$ouName'" -SearchBase $base -SearchScope OneLevel -ErrorAction SilentlyContinue)) {
        New-ADOrganizationalUnit -Name $ouName -Path $base -ProtectedFromAccidentalDeletion $false
      }
    }
    # Finance Admins group
    if (-not (Get-ADGroup -Filter "Name -eq 'Finance Admins'" -ErrorAction SilentlyContinue)) {
      New-ADGroup -Name "Finance Admins" -SamAccountName "Finance Admins" `
        -GroupCategory Security -GroupScope Global `
        -Path "OU=Finance,$base"
    }
    $users = @(
      @{Name="han.solo";  Password="Rebel1234!";    SPN=$null},
      @{Name="padme.amidala"; Password="Rebel1234!";   SPN=$null},
      @{Name="rebel_svc";     Password="RebelSvc2025!"; SPN="HTTP/rebel.local"}
    )
    foreach ($u in $users) {
      $secPwd = ConvertTo-SecureString $u.Password -AsPlainText -Force
      if (-not (Get-ADUser -Filter "SamAccountName -eq '$($u.Name)'" -ErrorAction SilentlyContinue)) {
        New-ADUser -Name $u.Name -SamAccountName $u.Name `
          -UserPrincipalName "$($u.Name)@rebel.local" `
          -AccountPassword $secPwd -Enabled $true -PasswordNeverExpires $true `
          -Path "CN=Users,$base"
        Write-Output "Created user: $($u.Name)"
      }
      if ($u.SPN) {
        Set-ADUser -Identity $u.Name -ServicePrincipalNames @{Add=$u.SPN} -ErrorAction SilentlyContinue
      }
      try { Add-ADGroupMember -Identity "Finance Admins" -Members $u.Name -ErrorAction SilentlyContinue } catch {}
    }
  register: finance_users
  changed_when: "'Created' in finance_users.stdout"
  when: ad_domain == "rebel.local"

# trade.corp users/groups (used for forest trust escalation paths)
- name: Create trade.corp OUs and users
  ansible.windows.win_shell: |
    Import-Module ActiveDirectory
    $PSDefaultParameterValues = @{ '*-AD*:Server' = 'localhost' }
    $base = "DC=trade,DC=corp"
    foreach ($ouName in @("Root","ServiceAccounts")) {
      if (-not (Get-ADOrganizationalUnit -Filter "Name -eq '$ouName'" -SearchBase $base -SearchScope OneLevel -ErrorAction SilentlyContinue)) {
        New-ADOrganizationalUnit -Name $ouName -Path $base -ProtectedFromAccidentalDeletion $false
      }
    }
    if (-not (Get-ADGroup -Filter "Name -eq 'Root Admins'" -ErrorAction SilentlyContinue)) {
      New-ADGroup -Name "Root Admins" -SamAccountName "Root Admins" `
        -GroupCategory Security -GroupScope Universal `
        -Path "OU=Root,$base"
    }
    $users = @(
      @{Name="yoda";     Password="Jedi1234!";   Groups=@("Domain Users")},
      @{Name="jedi_adm"; Password="JediAdm2025!"; Groups=@("Domain Users","Root Admins","Schema Admins","Enterprise Admins")}
    )
    foreach ($u in $users) {
      $secPwd = ConvertTo-SecureString $u.Password -AsPlainText -Force
      if (-not (Get-ADUser -Filter "SamAccountName -eq '$($u.Name)'" -ErrorAction SilentlyContinue)) {
        New-ADUser -Name $u.Name -SamAccountName $u.Name `
          -UserPrincipalName "$($u.Name)@trade.corp" `
          -AccountPassword $secPwd -Enabled $true -PasswordNeverExpires $true `
          -Path "OU=Root,$base"
        Write-Output "Created user: $($u.Name)"
      }
      foreach ($g in $u.Groups) {
        try { Add-ADGroupMember -Identity $g -Members $u.Name -ErrorAction SilentlyContinue } catch {}
      }
    }
  register: root_users
  changed_when: "'Created' in root_users.stdout"
  when: ad_domain == "trade.corp"
